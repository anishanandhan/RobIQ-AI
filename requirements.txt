# ── RUNTIME DEPENDENCIES ──────────────────────────────────────────────────────
# RobIQ-AI ranker itself has ZERO external runtime dependencies (uses only standard library).

# ── DEVELOPMENT & TESTING DEPENDENCIES ───────────────────────────────────────
pytest>=9.0.0
pytest-cov>=6.0.0
ruff>=0.2.0
mypy>=1.8.0
