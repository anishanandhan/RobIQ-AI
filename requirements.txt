# ── RUNTIME DEPENDENCIES ──────────────────────────────────────────────────────
# Core ranking (fast mode) has zero dependencies
# Enhanced features (hybrid/enhanced mode) require the packages below

# Enhanced ranking features (optional)
sentence-transformers>=2.2.0    # Semantic skill matching
anthropic>=0.34.0                # LLM career evaluation
python-docx>=1.1.0              # Job description parsing
numpy>=1.24.0                   # Numerical operations
scikit-learn>=1.3.0             # Cosine similarity
google-generativeai>=0.3.0      # Alternative LLM provider

# ── DEVELOPMENT & TESTING DEPENDENCIES ────────────────────────────────────────
pytest>=9.0.0
pytest-cov>=6.0.0
ruff>=0.2.0
mypy>=1.8.0
